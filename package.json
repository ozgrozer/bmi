{
  "name": "bmi",
  "scripts": {
    "dev": "parcel ./src/index.html --out-dir ./dist --public-url / --port 1280",
    "build": "parcel build ./src/index.html --out-dir ./dist --public-url / --no-source-maps"
  },
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-react": "^6.24.1",
    "parcel-bundler": "1.11.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}
